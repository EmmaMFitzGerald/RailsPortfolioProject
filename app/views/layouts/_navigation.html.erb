<nav class="navbar navbar-default">
  <%# <div class="container-fluid"> %>
    <div class="navbar-header">
      <a class="navbar-brand"><%= link_to "Memory Box", root_path %>
    </div>
    <% if current_user %>
    <ul class="nav navbar-nav">
      <li class="active"><%= link_to "Categories", categories_path %> 
    </ul>
    <ul class="nav navbar-nav">
      <li class="active"><%= link_to "Users", users_path %> 
    </ul>
    </ul> 
    <%= form_tag(search_page_path, :method => "get",   
    class: 'navbar-form navbar-left') do %>  
    <div class="input-group">  
      <%= search_field_tag :search, params[:search], placeholder: "Search for Child", class: "form-control" %>  
      <div class="input-group-btn">  
        <%= button_tag "Search", :class => 'btn btn-info', :name => nil%>  
      </div>  
    </div>  
  <% end %>
    <ul class="nav navbar-nav">
      <li class="active"><%= link_to "Edit Your Profile", edit_user_registration_path(@user)%>
    </ul>
  <% end%>
    <ul class="nav navbar-nav navbar-right">
        <% if !current_user %>
          <li><%= link_to "Sign Up", new_user_registration_path %><span class="glyphicon glyphicon-user"></span></li>
          <li><%= link_to "Log In", user_session_path %><span class="glyphicon glyphicon-log-in"></span></li>
        <% else %>
          <li> <%= link_to "Log Out", destroy_user_session_path, :method => :delete%><span class="glyphicon glyphicon-log-in"></span></li>
        <% end %>
    </ul>
  </div>
</nav>