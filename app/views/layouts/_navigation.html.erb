<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand"><%= link_to "Memory Box", root_path %>
    </div>
    <ul class="nav navbar-nav">
      <li class="active"><%= link_to "Search Memories by Category", categories_path %> 
    </ul>
    <ul class="nav navbar-nav">
      <li class="active"><%= link_to "All Users", users_path %> 
    </ul>
    </ul> 
    <%= form_tag(search_page_path, :method => "get",   
    class: 'navbar-form navbar-left') do %>  
    <div class="input-group">  
      <%= search_field_tag :search, params[:search], placeholder: "Search for Child", class: "form-control" %>  
      <div class="input-group-btn">  
        <%= button_tag "Search", :class => 'btn btn-info', :name => nil%>  
      </div>  
    </div>  
  <% end %>
    <ul class="nav navbar-nav navbar-right">
      <li>
        <% if !current_user %>
          <%= link_to "Sign Up", new_user_registration_path %><span class="glyphicon glyphicon-user"></span></li>
        <li>
         <%= link_to "Log In", user_session_path %><span class="glyphicon glyphicon-log-in"></span></li>
         <% else %>
          <%= link_to "Log Out", destroy_user_session_path, :method => :delete%><span class="glyphicon glyphicon-log-in"></span></li>
          <% end %>
    </ul>
  </div>
</nav>